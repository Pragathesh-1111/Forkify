{
  "name": "forkify",
  "version": "1.0.0",
  "description": "Receipe application",
  "author": "Pragatheshwaran",
  "default": "index.html",
  "scripts": {
    "start": "parcel index.html",
    "build": "npx parcel build index.html --dist-dir ./dist --public-url ./ --no-optimize"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.16.4",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "core-js": "^3.49.0",
    "fracty": "^1.0.12",
    "regenerator-runtime": "^0.14.1"
  }
}
